# AFM

Requirements: `..._train.csv` and `..._test.csv` datasets from Google Drive.

File explanation:
* `main.py`: Script to run the AFM modeling.
* `results`: Folder with the outputs of the scripts depending on the input file.
